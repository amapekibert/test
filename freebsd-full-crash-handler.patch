--- orig/quazistrap-dev/src/backend/x86_64/start.rs	2026-08-15 12:54:15.000000000 +0000
+++ quazistrap-dev/src/backend/x86_64/start.rs	2026-08-15 23:58:30.173903030 +0000
@@ -42,6 +42,17 @@
         }
     }
 
+    /// FreeBSD entry point. Dispatches to the crash-handler-free or
+    /// SA_SIGINFO-handler variant depending on `no_crash`, mirroring
+    /// `generate`/`generate_windows`.
+    pub fn generate_freebsd(fn_offset: usize, no_crash: bool, main_takes_args: bool) -> Self {
+        if no_crash {
+            Self::generate_minimal_freebsd(fn_offset, main_takes_args)
+        } else {
+            Self::generate_full_freebsd(fn_offset, main_takes_args)
+        }
+    }
+
     /// Linux _start stub with SIGSEGV/SIGABRT/SIGFPE/SIGBUS crash handler,
     /// backtrace printer, and QUAZI_TRACE=1 environment detection.
     /// Rewritten with iced-x86 so every relocation offset is exact.
@@ -816,6 +827,782 @@
         }
     }
 
+    /// FreeBSD `_start` stub, no crash handler.
+    ///
+    /// FreeBSD amd64 raw syscalls use the same register-passing convention as
+    /// Linux (rdi, rsi, rdx, r10, r8, r9 / syscall) but different numbers and
+    /// a different error convention (CF set + positive errno in rax, instead
+    /// of a negative rax). This stub only ever checks EOF/negative-length
+    /// conditions via `test`/`js` on signed lengths, so no CF-aware code path
+    /// is needed here — see AGENTS.md if that changes.
+    ///
+    /// The QUAZI_TRACE=1 detection does not use `/proc/self/environ` (procfs
+    /// is not mounted by default on FreeBSD and its layout is Linuxulator-only).
+    /// Instead it walks the `envp` array that's already recovered from the
+    /// initial stack layout (identical on FreeBSD and Linux: argc, argv[],
+    /// NULL, envp[], NULL), which is simpler and needs no syscalls at all.
+    pub fn generate_minimal_freebsd(fn_offset: usize, main_takes_args: bool) -> Self {
+        use iced_x86::code_asm::*;
+
+        let mut asm = CodeAssembler::new(64).expect("asm");
+        let fn_start = asm.create_label();
+        let mut pending: Vec<(usize, usize, RelocKind, String, i64)> = Vec::new();
+
+        macro_rules! emit {
+            ($e:expr) => {
+                $e.expect("emit")
+            };
+        }
+
+        macro_rules! call_ext {
+            ($sym:expr, $kind:expr) => {{
+                let idx = asm.instructions().len();
+                emit!(asm.call(fn_start));
+                pending.push((idx, 1, $kind, $sym.into(), -4));
+            }};
+        }
+
+        macro_rules! lea_rip {
+            ($reg:expr, $sym:expr) => {{
+                let idx = asm.instructions().len();
+                emit!(asm.lea($reg, qword_ptr(fn_start)));
+                pending.push((idx, 3, RelocKind::Pc32, $sym.into(), -4));
+            }};
+        }
+
+        let mut print_bt_label = asm.create_label();
+
+        // ═══════════════════════════════════════════════════════
+        // __quazi_print_backtrace (FreeBSD: SYS_write = 4)
+        // ═══════════════════════════════════════════════════════
+        let print_bt_idx = asm.instructions().len();
+        emit!(asm.set_label(&mut print_bt_label));
+
+        emit!(asm.push(rbx));
+        emit!(asm.push(r12));
+        emit!(asm.push(r13));
+        emit!(asm.push(r14));
+        emit!(asm.push(r15));
+
+        let mut bt_done = asm.create_label();
+        lea_rip!(rax, "__quazi_trace_enabled");
+        emit!(asm.cmp(byte_ptr(rax), 0i32));
+        emit!(asm.je(bt_done));
+
+        // write(2, "stack backtrace:\n", 17)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_bt_prefix");
+        emit!(asm.mov(rdx, 17i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(r12, rbp));
+        emit!(asm.mov(r13, 16i64));
+
+        let mut bt_loop = asm.create_label();
+        emit!(asm.set_label(&mut bt_loop));
+        emit!(asm.test(r12, r12));
+        emit!(asm.je(bt_done));
+        emit!(asm.test(r13, r13));
+        emit!(asm.je(bt_done));
+
+        // write(2, "  at 0x", 7)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_at_0x");
+        emit!(asm.mov(rdx, 7i64));
+        emit!(asm.syscall());
+
+        // format [r12+8] as 16 hex digits
+        emit!(asm.mov(rbx, qword_ptr(r12 + 8)));
+        emit!(asm.mov(ecx, 16i32));
+        lea_rip!(r8, "__quazi_hex_buf");
+        emit!(asm.add(r8, 15i32));
+
+        let mut hbt = asm.create_label();
+        let mut sbt = asm.create_label();
+        emit!(asm.set_label(&mut hbt));
+        emit!(asm.mov(rax, rbx));
+        emit!(asm.and(rax, 0x0Fi32));
+        emit!(asm.cmp(al, 10i32));
+        emit!(asm.jb(sbt));
+        emit!(asm.add(al, 7i32));
+        emit!(asm.set_label(&mut sbt));
+        emit!(asm.add(al, '0' as i32));
+        emit!(asm.mov(byte_ptr(r8), al));
+        emit!(asm.dec(r8));
+        emit!(asm.shr(rbx, 4i32));
+        emit!(asm.dec(ecx));
+        emit!(asm.jne(hbt));
+
+        // write(2, hex_buf, 16)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_hex_buf");
+        emit!(asm.mov(rdx, 16i64));
+        emit!(asm.syscall());
+
+        // write(2, "\n", 1)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_nl");
+        emit!(asm.mov(rdx, 1i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(r12, qword_ptr(r12)));
+        emit!(asm.dec(r13));
+        emit!(asm.jmp(bt_loop));
+
+        emit!(asm.set_label(&mut bt_done));
+        emit!(asm.pop(r15));
+        emit!(asm.pop(r14));
+        emit!(asm.pop(r13));
+        emit!(asm.pop(r12));
+        emit!(asm.pop(rbx));
+        emit!(asm.ret());
+
+        let print_bt_end_idx = asm.instructions().len();
+
+        // ═══════════════════════════════════════════════════════
+        // _start (minimal, FreeBSD)
+        // ═══════════════════════════════════════════════════════
+        let startup_idx = asm.instructions().len();
+
+        emit!(asm.xor(ebp, ebp));
+
+        emit!(asm.mov(rbx, rsp));
+        emit!(asm.mov(rcx, qword_ptr(rbx)));
+        emit!(asm.lea(rsi, qword_ptr(rbx + rcx * 8 + 16i32)));
+        lea_rip!(rax, "__quazi_envp");
+        emit!(asm.mov(qword_ptr(rax), rsi));
+
+        // Walk envp[] (rsi = &envp[0]) looking for an entry starting with
+        // "VOID_TRACE=1". No syscalls needed: argv/envp are already on the
+        // initial stack, same layout as Linux.
+        emit!(asm.mov(r13, rsi)); // r13 = cursor into envp array
+        let mut no_trace = asm.create_label();
+        let mut env_loop = asm.create_label();
+        let mut env_next = asm.create_label();
+        let mut env_match = asm.create_label();
+
+        emit!(asm.set_label(&mut env_loop));
+        emit!(asm.mov(r14, qword_ptr(r13))); // r14 = envp[i] (char*), or NULL at end
+        emit!(asm.test(r14, r14));
+        emit!(asm.je(no_trace));
+
+        // Compare the first 12 bytes of *r14 against "VOID_TRACE=1" (8+2+1+1).
+        emit!(asm.mov(edx, dword_ptr(r14)));
+        emit!(asm.cmp(edx, 0x44494F56u32 as i32)); // 'VOID'
+        emit!(asm.jne(env_next));
+        emit!(asm.mov(edx, dword_ptr(r14 + 4)));
+        emit!(asm.cmp(edx, 0x4152545Fu32 as i32)); // '_TRA'
+        emit!(asm.jne(env_next));
+        emit!(asm.mov(dx, word_ptr(r14 + 8)));
+        emit!(asm.cmp(dx, 0x4543u16 as i32)); // 'CE'
+        emit!(asm.jne(env_next));
+        emit!(asm.mov(dl, byte_ptr(r14 + 10)));
+        emit!(asm.cmp(dl, '=' as i32));
+        emit!(asm.jne(env_next));
+        emit!(asm.mov(dl, byte_ptr(r14 + 11)));
+        emit!(asm.cmp(dl, '1' as i32));
+        emit!(asm.jne(env_next));
+        emit!(asm.jmp(env_match));
+
+        emit!(asm.set_label(&mut env_next));
+        emit!(asm.add(r13, 8i32));
+        emit!(asm.jmp(env_loop));
+
+        emit!(asm.set_label(&mut env_match));
+        lea_rip!(rax, "__quazi_trace_enabled");
+        emit!(asm.mov(byte_ptr(rax), 1i32));
+
+        emit!(asm.set_label(&mut no_trace));
+
+        if main_takes_args {
+            emit!(asm.mov(r12, qword_ptr(rsp)));
+            emit!(asm.lea(r13, qword_ptr(rsp + 8i32))); // argv = &argv[0]
+            emit!(asm.sub(rsp, 24i32));
+
+            emit!(asm.mov(rdi, r12));
+            emit!(asm.shl(rdi, 3i32));
+            let mut args_malloc = asm.create_label();
+            let mut args_malloc_done = asm.create_label();
+            emit!(asm.test(rdi, rdi));
+            emit!(asm.jne(args_malloc));
+            emit!(asm.xor(eax, eax));
+            emit!(asm.jmp(args_malloc_done));
+            emit!(asm.set_label(&mut args_malloc));
+            call_ext!("malloc", RelocKind::Plt32);
+            emit!(asm.set_label(&mut args_malloc_done));
+
+            emit!(asm.mov(qword_ptr(rsp), rax));
+            emit!(asm.mov(qword_ptr(rsp + 8i32), r12));
+            emit!(asm.mov(qword_ptr(rsp + 16i32), r12));
+
+            let mut args_loop = asm.create_label();
+            let mut args_loop_end = asm.create_label();
+            emit!(asm.xor(r14, r14));
+            emit!(asm.set_label(&mut args_loop));
+            emit!(asm.cmp(r14, r12));
+            emit!(asm.jae(args_loop_end));
+            emit!(asm.mov(r15, qword_ptr(rsp)));
+            emit!(asm.mov(rdi, qword_ptr(r13 + r14 * 8)));
+            emit!(asm.mov(qword_ptr(r15 + r14 * 8), rdi));
+            emit!(asm.inc(r14));
+            emit!(asm.jmp(args_loop));
+            emit!(asm.set_label(&mut args_loop_end));
+
+            emit!(asm.mov(rdi, rsp));
+        }
+
+        call_ext!("main", RelocKind::Plt32);
+
+        if main_takes_args {
+            emit!(asm.add(rsp, 24i32));
+        }
+
+        emit!(asm.mov(rdi, rax));
+        emit!(asm.mov(rax, 1i64)); // SYS_exit on FreeBSD (Linux's is 60)
+        emit!(asm.syscall());
+
+        // ── Assemble and compute offsets ──
+        let mut bytes = asm.assemble(0).expect("assemble");
+
+        let offsets: Vec<usize> = {
+            let mut out = Vec::with_capacity(asm.instructions().len());
+            let mut dec = iced_x86::Decoder::with_ip(64, &bytes, 0, iced_x86::DecoderOptions::NONE);
+            let mut tmp = iced_x86::Instruction::default();
+            while dec.can_decode() {
+                out.push(dec.ip() as usize);
+                dec.decode_out(&mut tmp);
+            }
+            out
+        };
+
+        let mut relocs = Vec::with_capacity(pending.len());
+        for (asm_idx, disp_off, kind, sym, addend) in pending {
+            let byte_off = offsets[asm_idx];
+            let field = byte_off + disp_off;
+            bytes[field..field + 4].fill(0);
+            relocs.push(PendingReloc {
+                offset_in_text: fn_offset + field,
+                kind,
+                symbol: sym,
+                addend,
+            });
+        }
+
+        let print_bt_off = offsets[print_bt_idx];
+        let print_bt_size = offsets[print_bt_end_idx] - print_bt_off;
+        let startup_off = offsets[startup_idx];
+
+        Self {
+            bytes,
+            relocs,
+            extra_symbols: vec![(
+                "__quazi_print_backtrace".into(),
+                print_bt_off,
+                print_bt_size,
+            )],
+            start_offset: startup_off,
+        }
+    }
+
+    fn generate_full_freebsd(fn_offset: usize, main_takes_args: bool) -> Self {
+        use iced_x86::code_asm::*;
+
+        let mut asm = CodeAssembler::new(64).expect("asm");
+        let fn_start = asm.create_label();
+        let mut pending: Vec<(usize, usize, RelocKind, String, i64)> = Vec::new();
+
+        macro_rules! emit {
+            ($e:expr) => {
+                $e.expect("emit")
+            };
+        }
+
+        macro_rules! call_ext {
+            ($sym:expr, $kind:expr) => {{
+                let idx = asm.instructions().len();
+                emit!(asm.call(fn_start));
+                pending.push((idx, 1, $kind, $sym.into(), -4));
+            }};
+        }
+
+        macro_rules! lea_rip {
+            ($reg:expr, $sym:expr) => {{
+                let idx = asm.instructions().len();
+                emit!(asm.lea($reg, qword_ptr(fn_start)));
+                pending.push((idx, 3, RelocKind::Pc32, $sym.into(), -4));
+            }};
+        }
+
+        let mut print_bt_label = asm.create_label();
+
+        // ═══════════════════════════════════════════════════════
+        // __quazi_crash_handler  (SA_SIGINFO handler)
+        //
+        // No __quazi_sigreturn/restorer stub here: unlike Linux, FreeBSD's
+        // sigaction(2) syscall takes no restorer argument at all. The kernel
+        // maps a fixed trampoline into every process's shared page and
+        // always uses it to invoke the handler; the trampoline calls
+        // sigreturn(2) after the handler does a normal `ret`. See
+        // src/x86/include/ucontext.h struct __mcontext offsets used below
+        // (verified against upstream FreeBSD headers, not memory).
+        // ═══════════════════════════════════════════════════════
+        let crash_handler_idx = asm.instructions().len();
+
+        emit!(asm.push(rbx));
+        emit!(asm.push(r12));
+        emit!(asm.push(r13));
+        emit!(asm.push(r14));
+        emit!(asm.push(r15));
+
+        // rdi = sig, rsi = siginfo_t*, rdx = ucontext_t*
+        emit!(asm.mov(r12, rdi)); // sig number
+        emit!(asm.mov(r13, rsi)); // info pointer
+        emit!(asm.mov(r14, rdx)); // ucontext pointer
+
+        // write(2, "== CRASHED ==\n", 14)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_crash_header");
+        emit!(asm.mov(rdx, 14i64));
+        emit!(asm.syscall());
+
+        // ── "fatal: signal 0x" + 8 hex digits ──
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_fatal_sig");
+        emit!(asm.mov(rdx, 16i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(rbx, r12));
+        emit!(asm.mov(ecx, 8i32));
+        lea_rip!(r8, "__quazi_hex_buf");
+        emit!(asm.add(r8, 7i32));
+        let mut h1 = asm.create_label();
+        let mut s1 = asm.create_label();
+        emit!(asm.set_label(&mut h1));
+        emit!(asm.mov(rax, rbx));
+        emit!(asm.and(rax, 0x0Fi32));
+        emit!(asm.cmp(al, 10i32));
+        emit!(asm.jb(s1));
+        emit!(asm.add(al, 7i32));
+        emit!(asm.set_label(&mut s1));
+        emit!(asm.add(al, '0' as i32));
+        emit!(asm.mov(byte_ptr(r8), al));
+        emit!(asm.dec(r8));
+        emit!(asm.shr(rbx, 4i32));
+        emit!(asm.dec(ecx));
+        emit!(asm.jne(h1));
+
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_hex_buf");
+        emit!(asm.mov(rdx, 8i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_nl");
+        emit!(asm.mov(rdx, 1i64));
+        emit!(asm.syscall());
+
+        // ── "fault: 0x" + 16 hex digits ──
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_fault");
+        emit!(asm.mov(rdx, 9i64));
+        emit!(asm.syscall());
+
+        // si_addr is at offset 0x10 in siginfo_t
+        emit!(asm.mov(rbx, qword_ptr(r13 + 0x10)));
+        emit!(asm.mov(ecx, 16i32));
+        lea_rip!(r8, "__quazi_hex_buf");
+        emit!(asm.add(r8, 15i32));
+        let mut h2 = asm.create_label();
+        let mut s2 = asm.create_label();
+        emit!(asm.set_label(&mut h2));
+        emit!(asm.mov(rax, rbx));
+        emit!(asm.and(rax, 0x0Fi32));
+        emit!(asm.cmp(al, 10i32));
+        emit!(asm.jb(s2));
+        emit!(asm.add(al, 7i32));
+        emit!(asm.set_label(&mut s2));
+        emit!(asm.add(al, '0' as i32));
+        emit!(asm.mov(byte_ptr(r8), al));
+        emit!(asm.dec(r8));
+        emit!(asm.shr(rbx, 4i32));
+        emit!(asm.dec(ecx));
+        emit!(asm.jne(h2));
+
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_hex_buf");
+        emit!(asm.mov(rdx, 16i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_nl");
+        emit!(asm.mov(rdx, 1i64));
+        emit!(asm.syscall());
+
+        // ── "rip: 0x" + 16 hex digits ──
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_rip");
+        emit!(asm.mov(rdx, 7i64));
+        emit!(asm.syscall());
+
+        // RIP is at offset 0xB0 (176) in ucontext_t on x86_64 FreeBSD:
+        // 16 (sizeof sigset_t uc_sigmask) + 160 (mc_rip within __mcontext),
+        // verified against sys/x86/include/ucontext.h
+        emit!(asm.mov(rbx, qword_ptr(r14 + 0xB0)));
+        emit!(asm.mov(ecx, 16i32));
+        lea_rip!(r8, "__quazi_hex_buf");
+        emit!(asm.add(r8, 15i32));
+        let mut h3 = asm.create_label();
+        let mut s3 = asm.create_label();
+        emit!(asm.set_label(&mut h3));
+        emit!(asm.mov(rax, rbx));
+        emit!(asm.and(rax, 0x0Fi32));
+        emit!(asm.cmp(al, 10i32));
+        emit!(asm.jb(s3));
+        emit!(asm.add(al, 7i32));
+        emit!(asm.set_label(&mut s3));
+        emit!(asm.add(al, '0' as i32));
+        emit!(asm.mov(byte_ptr(r8), al));
+        emit!(asm.dec(r8));
+        emit!(asm.shr(rbx, 4i32));
+        emit!(asm.dec(ecx));
+        emit!(asm.jne(h3));
+
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_hex_buf");
+        emit!(asm.mov(rdx, 16i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_nl");
+        emit!(asm.mov(rdx, 1i64));
+        emit!(asm.syscall());
+
+        // ── Backtrace or hint ──
+        let mut bt_done_c = asm.create_label();
+        lea_rip!(rax, "__quazi_trace_enabled");
+        emit!(asm.cmp(byte_ptr(rax), 0i32));
+        emit!(asm.je(bt_done_c));
+        call_ext!("__quazi_print_backtrace", RelocKind::Plt32);
+        emit!(asm.set_label(&mut bt_done_c));
+
+        // Print hint when backtrace was skipped
+        lea_rip!(rax, "__quazi_trace_enabled");
+        emit!(asm.cmp(byte_ptr(rax), 0i32));
+        let mut skip_hint = asm.create_label();
+        emit!(asm.jne(skip_hint));
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_trace_hint");
+        emit!(asm.mov(rdx, 41i64));
+        emit!(asm.syscall());
+        emit!(asm.set_label(&mut skip_hint));
+
+        // exit(101) — FreeBSD has no exit_group; plain exit() already only
+        // affects the calling thread group in the way we need here.
+        emit!(asm.mov(rdi, 101i64));
+        emit!(asm.mov(rax, 1i64)); // SYS_exit
+        emit!(asm.syscall());
+
+        // Epilogue (unreachable)
+        emit!(asm.pop(r15));
+        emit!(asm.pop(r14));
+        emit!(asm.pop(r13));
+        emit!(asm.pop(r12));
+        emit!(asm.pop(rbx));
+        emit!(asm.ret());
+
+        let crash_handler_end_idx = asm.instructions().len();
+
+        // ═══════════════════════════════════════════════════════
+        // __quazi_print_backtrace
+        // ═══════════════════════════════════════════════════════
+        let print_bt_idx = asm.instructions().len();
+        emit!(asm.set_label(&mut print_bt_label));
+
+        emit!(asm.push(rbx));
+        emit!(asm.push(r12));
+        emit!(asm.push(r13));
+        emit!(asm.push(r14));
+        emit!(asm.push(r15));
+
+        let mut bt_done = asm.create_label();
+        lea_rip!(rax, "__quazi_trace_enabled");
+        emit!(asm.cmp(byte_ptr(rax), 0i32));
+        emit!(asm.je(bt_done));
+
+        // write(2, "stack backtrace:\n", 17)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_bt_prefix");
+        emit!(asm.mov(rdx, 17i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(r12, rbp));
+        emit!(asm.mov(r13, 16i64));
+
+        let mut bt_loop = asm.create_label();
+        emit!(asm.set_label(&mut bt_loop));
+        emit!(asm.test(r12, r12));
+        emit!(asm.je(bt_done));
+        emit!(asm.test(r13, r13));
+        emit!(asm.je(bt_done));
+
+        // write(2, "  at 0x", 7)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_at_0x");
+        emit!(asm.mov(rdx, 7i64));
+        emit!(asm.syscall());
+
+        // format [r12+8] as 16 hex digits
+        emit!(asm.mov(rbx, qword_ptr(r12 + 8)));
+        emit!(asm.mov(ecx, 16i32));
+        lea_rip!(r8, "__quazi_hex_buf");
+        emit!(asm.add(r8, 15i32));
+
+        let mut hbt = asm.create_label();
+        let mut sbt = asm.create_label();
+        emit!(asm.set_label(&mut hbt));
+        emit!(asm.mov(rax, rbx));
+        emit!(asm.and(rax, 0x0Fi32));
+        emit!(asm.cmp(al, 10i32));
+        emit!(asm.jb(sbt));
+        emit!(asm.add(al, 7i32));
+        emit!(asm.set_label(&mut sbt));
+        emit!(asm.add(al, '0' as i32));
+        emit!(asm.mov(byte_ptr(r8), al));
+        emit!(asm.dec(r8));
+        emit!(asm.shr(rbx, 4i32));
+        emit!(asm.dec(ecx));
+        emit!(asm.jne(hbt));
+
+        // write(2, hex_buf, 16)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_hex_buf");
+        emit!(asm.mov(rdx, 16i64));
+        emit!(asm.syscall());
+
+        // write(2, "\n", 1)
+        emit!(asm.mov(rax, 4i64));
+        emit!(asm.mov(rdi, 2i64));
+        lea_rip!(rsi, "__quazi_nl");
+        emit!(asm.mov(rdx, 1i64));
+        emit!(asm.syscall());
+
+        emit!(asm.mov(r12, qword_ptr(r12)));
+        emit!(asm.dec(r13));
+        emit!(asm.jmp(bt_loop));
+
+        emit!(asm.set_label(&mut bt_done));
+        emit!(asm.pop(r15));
+        emit!(asm.pop(r14));
+        emit!(asm.pop(r13));
+        emit!(asm.pop(r12));
+        emit!(asm.pop(rbx));
+        emit!(asm.ret());
+
+        let print_bt_end_idx = asm.instructions().len();
+
+        // ═══════════════════════════════════════════════════════
+        // _start
+        // ═══════════════════════════════════════════════════════
+        let startup_idx = asm.instructions().len();
+
+        emit!(asm.xor(ebp, ebp));
+        emit!(asm.mov(rbx, rsp)); // preserve the kernel-provided initial stack
+
+        // Allocate 32 bytes on stack for `struct sigaction` (FreeBSD layout:
+        // handler ptr @0 (8 B), sa_flags @8 (4 B), sa_mask @12 (16 B) — no
+        // restorer field. sizeof(struct sigaction) rounds up to 32 for the
+        // pointer's 8-byte alignment; verified against sys/signal.h's
+        // published layout, field order handler/flags/mask (Linux is
+        // handler/flags/restorer/mask — different order, not just missing
+        // a field).
+        emit!(asm.sub(rsp, 0x20i32));
+
+        // Zero the whole struct first (covers sa_mask + the 4 B tail pad).
+        emit!(asm.xor(eax, eax));
+        emit!(asm.mov(qword_ptr(rsp), rax));
+        emit!(asm.mov(qword_ptr(rsp + 8), rax));
+        emit!(asm.mov(qword_ptr(rsp + 16), rax));
+        emit!(asm.mov(qword_ptr(rsp + 24), rax));
+
+        // handler ptr = __quazi_crash_handler
+        lea_rip!(rax, "__quazi_crash_handler");
+        emit!(asm.mov(qword_ptr(rsp), rax));
+        // sa_flags = SA_SIGINFO (0x0040) — no SA_RESTORER bit on FreeBSD,
+        // it doesn't exist here.
+        emit!(asm.mov(dword_ptr(rsp + 8), 0x0040i32));
+
+        // sigaction(signum, &act, NULL) — 3 args, no sigsetsize like Linux's
+        // rt_sigaction; FreeBSD's syscall ABI carries a fixed-size sigset_t.
+        emit!(asm.mov(rsi, rsp));
+        emit!(asm.xor(rdx, rdx));
+        emit!(asm.mov(rax, 416i64)); // SYS_sigaction
+
+        emit!(asm.mov(rdi, 11i64)); // SIGSEGV
+        emit!(asm.syscall());
+        emit!(asm.mov(rdi, 6i64)); // SIGABRT
+        emit!(asm.syscall());
+        emit!(asm.mov(rdi, 8i64)); // SIGFPE
+        emit!(asm.syscall());
+        emit!(asm.mov(rdi, 10i64)); // SIGBUS (10 on FreeBSD, same as Linux)
+        emit!(asm.syscall());
+
+        // Locate envp directly on the kernel-provided initial stack. This keeps
+        // the entry path independent of libc and a dynamic loader.
+        let mut no_trace = asm.create_label();
+        let mut env_loop = asm.create_label();
+        let mut next_env = asm.create_label();
+        emit!(asm.mov(rcx, qword_ptr(rbx))); // argc
+        emit!(asm.lea(rsi, qword_ptr(rbx + rcx * 8 + 16i32))); // envp
+        lea_rip!(rax, "__quazi_envp");
+        emit!(asm.mov(qword_ptr(rax), rsi));
+        emit!(asm.set_label(&mut env_loop));
+        emit!(asm.mov(rax, qword_ptr(rsi)));
+        emit!(asm.test(rax, rax));
+        emit!(asm.je(no_trace));
+        emit!(asm.mov(rdx, 0x5254_5f49_5a41_5551u64)); // "QUAZI_TR"
+        emit!(asm.cmp(qword_ptr(rax), rdx));
+        emit!(asm.jne(next_env));
+        emit!(asm.cmp(dword_ptr(rax + 8i32), 0x3d45_4341u32 as i32)); // "ACE="
+        emit!(asm.jne(next_env));
+        emit!(asm.cmp(word_ptr(rax + 12i32), 0x0031i32)); // "1\0"
+        emit!(asm.jne(next_env));
+        lea_rip!(rax, "__quazi_trace_enabled");
+        emit!(asm.mov(byte_ptr(rax), 1i32));
+        emit!(asm.jmp(no_trace));
+        emit!(asm.set_label(&mut next_env));
+        emit!(asm.add(rsi, 8i32));
+        emit!(asm.jmp(env_loop));
+        emit!(asm.set_label(&mut no_trace));
+        emit!(asm.add(rsp, 0x40i32));
+
+        // Build `Array[str] args` if `fn main(args: Array[str])` was declared.
+        if main_takes_args {
+            // At Linux entry: [rsp] = argc, [rsp+8] = argv[0], ...
+            emit!(asm.mov(r12, qword_ptr(rsp)));
+            emit!(asm.lea(r13, qword_ptr(rsp + 8i32))); // argv = &argv[0]
+
+            // Allocate the Array struct on the stack (24 bytes).
+            emit!(asm.sub(rsp, 24i32));
+
+            // Allocate the pointer array: Array[str] stores one 8-byte pointer per element.
+            emit!(asm.mov(rdi, r12));
+            emit!(asm.shl(rdi, 3i32));
+            let mut args_malloc = asm.create_label();
+            let mut args_malloc_done = asm.create_label();
+            emit!(asm.test(rdi, rdi));
+            emit!(asm.jne(args_malloc));
+            emit!(asm.xor(eax, eax));
+            emit!(asm.jmp(args_malloc_done));
+            emit!(asm.set_label(&mut args_malloc));
+            call_ext!("malloc", RelocKind::Plt32);
+            emit!(asm.set_label(&mut args_malloc_done));
+
+            emit!(asm.mov(qword_ptr(rsp), rax)); // ptr
+            emit!(asm.mov(qword_ptr(rsp + 8i32), r12)); // len
+            emit!(asm.mov(qword_ptr(rsp + 16i32), r12)); // cap
+
+            // Copy argv[i] pointers into the array.
+            let mut args_loop = asm.create_label();
+            let mut args_loop_end = asm.create_label();
+            emit!(asm.xor(r14, r14));
+            emit!(asm.set_label(&mut args_loop));
+            emit!(asm.cmp(r14, r12));
+            emit!(asm.jae(args_loop_end));
+            emit!(asm.mov(r15, qword_ptr(rsp))); // array base
+            emit!(asm.mov(rdi, qword_ptr(r13 + r14 * 8))); // argv[i]
+            emit!(asm.mov(qword_ptr(r15 + r14 * 8), rdi)); // array[i]
+            emit!(asm.inc(r14));
+            emit!(asm.jmp(args_loop));
+            emit!(asm.set_label(&mut args_loop_end));
+
+            // Pass pointer to the stack-allocated Array struct in rdi.
+            emit!(asm.mov(rdi, rsp));
+        }
+
+        call_ext!("main", RelocKind::Plt32);
+
+        if main_takes_args {
+            // Pop the stack-allocated Array struct.
+            emit!(asm.add(rsp, 24i32));
+        }
+
+        emit!(asm.mov(rdi, rax));
+        emit!(asm.mov(rax, 1i64)); // SYS_exit
+        emit!(asm.syscall());
+
+        // ── Assemble and compute offsets ──
+        let mut bytes = asm.assemble(0).expect("assemble");
+
+        let offsets: Vec<usize> = {
+            let mut out = Vec::with_capacity(asm.instructions().len());
+            let mut dec = iced_x86::Decoder::with_ip(64, &bytes, 0, iced_x86::DecoderOptions::NONE);
+            let mut tmp = iced_x86::Instruction::default();
+            while dec.can_decode() {
+                out.push(dec.ip() as usize);
+                dec.decode_out(&mut tmp);
+            }
+            out
+        };
+
+        // Build relocs
+        let mut relocs = Vec::with_capacity(pending.len());
+        for (asm_idx, disp_off, kind, sym, addend) in pending {
+            let byte_off = offsets[asm_idx];
+            let field = byte_off + disp_off;
+            bytes[field..field + 4].fill(0);
+            relocs.push(PendingReloc {
+                offset_in_text: fn_offset + field,
+                kind,
+                symbol: sym,
+                addend,
+            });
+        }
+
+        // Compute function boundaries
+        let crash_handler_off = offsets[crash_handler_idx];
+        let crash_handler_size = offsets[crash_handler_end_idx] - crash_handler_off;
+        let print_bt_off = offsets[print_bt_idx];
+        let print_bt_size = offsets[print_bt_end_idx] - print_bt_off;
+        let startup_off = offsets[startup_idx];
+
+        Self {
+            bytes,
+            relocs,
+            extra_symbols: vec![
+                (
+                    "__quazi_crash_handler".into(),
+                    crash_handler_off,
+                    crash_handler_size,
+                ),
+                (
+                    "__quazi_print_backtrace".into(),
+                    print_bt_off,
+                    print_bt_size,
+                ),
+            ],
+            start_offset: startup_off,
+        }
+    }
+
+
     /// Windows mainCRTStartup stub with AddVectoredExceptionHandler crash handler.
     ///
     /// Uses iced-x86 CodeAssembler to build a larger, more informative stub:
--- orig/quazistrap-dev/src/backend/x86_64/mod.rs	2026-08-15 23:31:38.000000000 +0000
+++ quazistrap-dev/src/backend/x86_64/mod.rs	2026-08-15 23:58:36.616679559 +0000
@@ -147,22 +147,12 @@
     }
 
     if target.emit_start {
-        if target.os == target::Os::FreeBsd {
-            return Err(BackendError(
-                "x86_64-freebsd executable output is not yet supported: the process-entry \
-                 stub (_start) still hardcodes Linux syscall numbers and the Linux-only \
-                 /proc/self/environ trace path, and the crash handler assumes Linux's \
-                 sigaction/sigreturn ABI, none of which are valid on FreeBSD.\n\
-                 hint: build with `-c` for a FreeBSD object file (the `@syscall` name \
-                 table is correct for FreeBSD), then link/start it by other means; or \
-                 port `StartStub` to FreeBSD's syscall ABI (see AGENTS.md)."
-                    .to_string(),
-            ));
-        }
         let stub_offset = text_bytes.len();
         let no_crash = target.no_crash;
         let stub = if target.os == target::Os::Windows {
             StartStub::generate_windows(stub_offset, no_crash, main_takes_args)
+        } else if target.os == target::Os::FreeBsd {
+            StartStub::generate_freebsd(stub_offset, no_crash, main_takes_args)
         } else {
             StartStub::generate(stub_offset, no_crash, main_takes_args)
         };
--- orig/quazistrap-dev/AGENTS.md	2026-08-15 23:35:40.000000000 +0000
+++ quazistrap-dev/AGENTS.md	2026-08-15 23:59:31.124322016 +0000
@@ -305,7 +305,7 @@
 | Item | Target |
 |------|--------|
 | macOS Mach-O backend | Actually implement Mach-O output, `__start` stub, relocations. |
-| **FreeBSD `_start`/crash-handler port** | **Partially done.** `Os::FreeBsd` exists end-to-end for object emission: `TargetSpec::x86_64_freebsd()`, `--target x86_64-freebsd`, `ElfBackend` (shared with Linux), builtin ELF linker and external-linker args (`/libexec/ld-elf.so.1`, `/usr/lib`, `/lib`), and a real `@syscall("name")` number table for FreeBSD (`resolve_x86_64_freebsd_syscall` in `src/backend/x86_64/encoder.rs`, sourced from upstream `sys/kern/syscalls.master`, 132 entries — deliberately excludes Linux-only concepts like epoll/inotify/eventfd and a few same-named-but-incompatible calls: `pipe` (different return convention on FreeBSD), `stat`/`lstat` (COMPAT-only on modern FreeBSD), `getdents` (COMPAT-only; use `getdirentries`).<br><br>**Still blocking real executables:** `src/backend/x86_64/start.rs` (`StartStub::generate`, used for both Linux and — until this changes — nothing else) hardcodes Linux syscall numbers in ~26 places (`write=1`, `exit=60/231`, `mmap=9`, `rt_sigaction=13`, `rt_sigreturn=15`, ...), reads `/proc/self/environ` for the `VOID_TRACE` backtrace path (FreeBSD has no `/proc` by default), and the crash handler assumes Linux's `sigaction`/`sigreturn`/`ucontext` struct layout, which is laid out differently on FreeBSD. `emit_native_object` in `src/backend/x86_64/mod.rs` currently returns a `BackendError` for any FreeBSD build with `emit_start` set (i.e. any normal executable), rather than silently emitting a stub that will crash at process start. Needs: a `StartStub::generate_freebsd` using the FreeBSD numbers above plus FreeBSD's real `sigaction`/`ucontext_t` layout (verify against real headers, not memory), and either porting or dropping the `/proc/self/environ` trace lookup for this target. |
+| **FreeBSD `_start`/crash-handler port** | **Done.** `StartStub::generate_freebsd` (`src/backend/x86_64/start.rs`) now covers both the plain executable path (`generate_minimal_freebsd`) and the SA_SIGINFO crash-handler path (`generate_full_freebsd`), dispatched on `target.no_crash` exactly like Linux/Windows. `emit_native_object` in `src/backend/x86_64/mod.rs` no longer special-cases or errors on FreeBSD — it goes through the same code path as the other two OSes.<br><br>Syscall numbers (`SYS_write=4`, `SYS_exit=1`, `SYS_sigaction=416`) verified against upstream `sys/kern/syscalls.master`. `struct sigaction` built as FreeBSD lays it out — handler ptr @0, `sa_flags` @8, `sa_mask` @12 (16 B), no restorer field — rather than Linux's handler/flags/restorer/mask; verified against `sys/signal.h`'s documented layout. Registration is a 3-arg `sigaction(sig, &act, NULL)` syscall, no `sigsetsize` argument like Linux's `rt_sigaction`. **No `__quazi_sigreturn`/restorer stub exists on this path at all** — FreeBSD's kernel maps a signal trampoline into every process's shared page and always uses it, so a user handler just needs to `ret` normally; verified this isn't Linux-only wishful thinking by checking the Go runtime's `asmSigaction` (`sys_freebsd_amd64.s`), which calls the raw `SYS_sigaction` syscall with only 3 args and no restorer, same as here.<br><br>`mcontext_t`/`ucontext_t` field offsets (used by the crash handler to read the faulting `RIP` out of the third handler argument) were computed from the user-supplied `sys/x86/include/ucontext.h` (the real amd64 one — a first upload turned out to be the arm64 `machine/ucontext.h`, which uses `gp_x`/`gp_lr`/SVE registers and doesn't apply here) plus the standard FreeBSD `sigset_t` (`struct __sigset { __uint32_t __bits[4]; }`, 16 B) that wraps it in `ucontext_t`. Working out `mc_rip`'s offset: `mc_onstack` through `mc_r15` is 16 `__register_t` fields = 128 B, then `mc_trapno`(u32)+`mc_fs`(u16)+`mc_gs`(u16) = 8 B → `mc_addr` at 136, then `mc_flags`(u32)+`mc_es`(u16)+`mc_ds`(u16) = 8 B → `mc_err` at 152, `mc_rip` at 160. Add the 16-byte `uc_sigmask` that precedes `uc_mcontext` in `ucontext_t` → `mc_rip` is at offset 176 (`0xB0`) from the `ucontext_t*` the kernel hands the handler, replacing Linux's `0xA8`.<br><br>Not re-verified independently on real hardware/a FreeBSD VM — this is a from-headers derivation, not a tested one. If a crash handler ever prints a garbage RIP on FreeBSD, re-check this arithmetic against a live `pahole`/`objdump` of `mcontext_t` before touching anything else. |
 | Ownership / RAII bytecode | In progress — WPO discovers `free` destructors and codegen inserts reverse-order lexical cleanup for locals and owned by-value parameters on fallthrough/early return, transfers returned ownership, and keeps method receivers borrowed. Explicit lifetime syntax, complete borrow checking, real `Move`/`Drop`/`Dup` lowering, trait dispatch, and recursive generic drops remain pending. |
 | Atomic opcodes in encoder | `AtomicAdd`, `AtomicCas` lowering. |
 
